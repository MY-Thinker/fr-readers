import type { ExpertVideo } from "./types";

/**
 * 专家视频列表（预留）
 * 管理员可在 /admin/videos 页面通过后台上传优质专家视频；
 * 上传后视频数据写入此处，并在 /lectures 页面的视频播放区展示。
 *
 * 当前阶段：空数组占位，等待后续接入真实后端上传接口。
 */
export const expertVideos: ExpertVideo[] = [];
